<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.todolist.mgnt.tl.dao.TodoListMapper">

	<!-- 테스트 -->
	<select id="todoListTest" parameterType="String" resultType="java.lang.String">
		<![CDATA[
			SELECT 'test' as result
			FROM dual
			WHERE 1=1
		]]>
	</select>
	

</mapper>